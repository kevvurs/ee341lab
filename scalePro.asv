% kevin
% scale with averages points.
function [img_min] = scalePro(img_filename, S)
    img = imread(img_filename);
    img_alt = double(img);
    v_ub = floor(size(img_alt,1)/S);
    h_ub = floor(size(img_alt,2)/S);
    for i = 1:a_ub
        img_alt(1:S:end, round(S/2):S:end)
        for j = 1:S
           
        end
    end
    img_min = uint8(img_alt(round(S/2):S:end, round(S/2):S:end));
    return;
end